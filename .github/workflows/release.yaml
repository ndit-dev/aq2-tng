name: TNG Release

# on:
#   push:
#     tags:
#       - '*'

on:
  workflow_dispatch:

jobs:
  generate_release:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        builds: ["tng-lin-x86_64", "tng-lin-arm64", "tng-win-32", "tng-win-64", "tng-darwin-x86_64"]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          repository: actionquake/aq2-tng
          ref: ${{ github.ref }}

      - name: version
        run: echo "::set-output name=version::$(git rev-parse --short HEAD)"
        id: version
        
      - name: release
        uses: actions/create-release@v1
        id: create_release
        with:
          draft: false
          prerelease: false
          release_name: aq2-tng_${{ steps.version.outputs.version }}
          tag_name: ${{ steps.version.outputs.version }}
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Download ${{ matrix.builds }} build artifacts
        uses: dawidd6/action-download-artifact@v2
        with:
          workflow: build.yaml
          name: ${{ matrix.builds }}
          skip_unpack: true
      
      - name: Upload dist tarball to release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          body_path: 
          tag_name: ${{ steps.version.outputs.version }}
          files: |
            tng-win-64.zip
            tng-win-32.zip
            tng-lin-arm64.zip
            tng-lin-x86_64.zip
            tng-darwin-x86_64.zip
